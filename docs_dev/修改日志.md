# 规则
该文档自动更新

# 涉及内容
- 每一次文件和代码修改

例如：
说明：以下示例仅用于格式参考，不计入 `# 正文` 记录。
规则：`# 正文` 下仅存放同结构的独立记录，且每条记录使用一个 `## 时间` 标题，不与格式说明混写。
## 2026.3.10
### 序号（同一天中的） 具体的修改名称
- 0.05alpha
- 详细要点：
1. 
2. 
3. 

# 正文

## 2026.3.11
### 1 交流规范引用格式调整与测试方案文档化
- 0.5.1-alpha
- 详细要点：
1. 交流规范改为 Markdown 链接格式并强制带行号，确保 VSCode 插件可点击跳转。
2. 新增自动化测试扩展方案文档（明确不包含 API 相关测试）。
3. 补充本次文档变更的测试记录为“未执行测试”。

### 2 自动化测试扩展实现与命令说明补充
- 0.5.1-alpha
- 详细要点：
1. `build.gradle` 新增 JUnit 标签分层任务与 Jacoco 覆盖率汇总任务。
2. 新增稳定核心与集成测试用例，并补充 SNBT fixture 作为兼容性样例。
3. CI `build.yml` 增加扩展测试/覆盖率开关与报告归档。
4. 修复测试文件中文注释乱码，统一为中文说明。
5. `docs_dev/使用说明.md` 补充测试命令使用方式与覆盖率入口。

### 3 测试记录格式调整与技能规范同步
- 0.5.1-alpha
- 详细要点：
1. `docs_dev/测试记录.md` 调整为仅日期标题格式。
2. `skills/docs-dev-docflow/SKILL.md` 同步模板与必填字段口径。

### 4 自动化测试扩展的测试与审查记录补充
- 0.5.1-alpha
- 详细要点：
1. `docs_dev/测试记录.md` 更新为测试通过与覆盖范围说明。
2. `docs_dev/审查日志.md` 补充审查要点与结论记录。

## 2026.3.10
### 1 后续迭代与索引文档迁移
- 0.05alpha
- 详细要点：
1. 将历史路线文档中的计划、测试、风险、经验索引拆分迁移到 `docs_`。
2. `docs_/更新计划草稿与索引.md` 新增文档定位、迁移说明、执行计划与关联索引。
3. `docs_/测试矩阵.md` 与 `docs_/测试记录.md` 建立分层矩阵与执行记录入口。
4. `docs_/保留问题.md` 与 `docs_/经验总结.md` 完成风险和经验模块收敛。

### 2 GUI 当前匹配显示截断与 tooltip 提示
- 0.05alpha
- 详细要点：
1. 主界面“当前连接”列表按宽度截断并显示 `…(+N)`。
2. tooltip 同样截断，最多显示 100 个目标，并显示剩余数量。
3. 展示逻辑统一到配对界面通用渲染路径中，避免多处重复处理。

### 3 命令输出前缀对齐
- 0.05alpha
- 详细要点：
1. `audit` 文本输出统一补齐 `[RedstoneLink]` 前缀。
2. `audit summary csv` 输出同样对齐前缀，便于统一识别与检索。

### 4 命令序号有效性过滤
- 0.05alpha
- 详细要点：
1. GUI 与命令侧统一复用序号解析逻辑。
2. 命令侧补齐序号有效性校验（已分配且未退役）。
3. 排除 `node get` / `link get` 的序号有效性过滤需求。

### 5 服务端序号校验工具抽取
- 0.05alpha
- 详细要点：
1. 新增服务端校验工具类集中复用校验逻辑。
2. 命令侧改为统一调用工具类，移除本地私有实现。

### 6 构建代理配置修复
- 0.05alpha
- 详细要点：
1. `gradle.properties` 显式配置 HTTP/HTTPS 代理。

### 7 构建镜像补充
- 0.05alpha
- 详细要点：
1. `build.gradle` 补充腾讯、华为、网易镜像仓库。
2. `settings.gradle` 补充插件解析镜像仓库。

### 8 国内镜像优先与官方兜底
- 0.05alpha
- 详细要点：
1. Loom 资源与版本清单默认走国内镜像。
2. 提供命令参数支持按需切回官方地址。

### 9 Loom 默认源回切官方（修复单镜像失效）
- 0.05alpha
- 详细要点：
1. Loom 默认地址回切 Mojang 官方源，避免单镜像不可达导致失败。
2. 保留命令行参数覆盖能力，按需切回国内镜像。


### 10 物品栏当前连接目标悬停截断
- 0.5.1-alpha
- 详细要点：
1. 物品栏“当前连接”提示按字符数截断并显示 `…(+N)`。
2. `LinkerItem` 与 `PairableBlockItem` 复用 `TooltipTextTruncateUtil`，默认最大 48 字符。
3. 修正物品相关类文件为 UTF-8 无 BOM，修复编译报错。

## 2026.3.09
### 1 代码注释补全与乱码复核
- 0.05alpha
- 详细要点：
1. 补齐核心类/方法悬停注释（Javadoc），统一为中文技术说明。
2. 扫描源码与文档范围乱码特征，记录当前复核结论。
3. 保持“仅补注释，不改业务语义”的最小改动策略。



