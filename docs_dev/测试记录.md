# 规则
- 该文档自动更新

# 涉及内容
- 测试结果记录

# 格式说明：
例如：
说明：以下示例仅用于格式参考，不计入 `# 正文` 记录。
规则：`# 正文` 下仅存放同结构的独立记录，且每条记录使用一个 `## 时间` 标题，不与格式说明混写。
## 2026.3.10 11:28
### 序号（同一天内） 具体的经验名称
- 0.05alpha
- 详细要点：
1. 
2. 
3. 
- 测试结果：

# 正文

## 2026.3.10
### 1 分层自动化测试与回归基线迁移
- 0.05alpha
- 详细要点：
1. 回归基线标准：原版单路高频最快 `2 tick/周期`。
2. 将原路线文档中的测试矩阵与回归基线拆分入 `docs_/测试矩阵.md` 与当前文件。
3. 保留执行顺序与验证口径，统一后续手工回归记录入口。
4. 将 `diag lithium` 纳入后续固定记录项。
5. 执行检查清单：统一命名与输出语义是否符合当前规范。
6. 执行检查清单：命令序号有效性过滤是否覆盖异常输入。
7. 执行检查清单：数据层有效性和内容校验是否通过。
8. 执行检查清单：本地联机矩阵与服务端回归是否有证据归档。
9. 执行检查清单：高频并发场景是否存在错态、卡死或不同步。
- 测试结果：
1. 本次为文档迁移，不涉及代码行为变更。
2. 待下一轮 E0~E7 实测后补充结果明细。

## 2026.3.10 17:00
### 2 GUI 当前匹配显示截断与 tooltip 提示
- 0.05alpha
- 详细要点：
1. 主界面“当前连接”列表按宽度截断并显示 `…(+N)`。
2. tooltip 同样截断，最多显示 100 个目标，并显示剩余数量。
3. 变更仅涉及客户端展示逻辑。
- 测试结果：
1. 未执行测试。
2. 待按 E0~E7 回归路径补充验证记录。
