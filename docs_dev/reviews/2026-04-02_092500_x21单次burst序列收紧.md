# x21单次burst序列收紧审查

## 1. 功能与语义、架构设计（关键方法和入口）
- 本次改动只收紧 `x21` 的单次 burst 样本，把 `start_sync_latency_single_burst.sequence` 从平台型序列改为逐 tick 递减并以 `0` 收尾，保持 `tickCount=8`、source/core 延迟断言和窗口覆盖断言不变，属于最小粒度测试样本增强。[sync_samechunk_latency_1024_to_1024_banded16_single_burst.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/cases/functional/x21/sync_samechunk_latency_1024_to_1024_banded16_single_burst.json#L66) [sync_samechunk_latency_1024_to_1024_banded16_single_burst.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/cases/functional/x21/sync_samechunk_latency_1024_to_1024_banded16_single_burst.json#L107)
- trace 侧仍通过 `sync_custom` 模板按 `tickCount` 展开逐 tick 期望值，因此这次改动不会改变测试框架，只会改变被比对的 8 tick 样本内容。[Bench.FunctionalTrace.ps1](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/lib/Bench.FunctionalTrace.ps1#L1431)

## 2. 关键数据结构和算法性能分析
- 本次没有新增数据结构，也没有改动调度或 trace 的算法路径；变化仅体现在 8 个 power 样本的内容。`expectedPowers` 仍由现有流程生成并传给 `sync_latency` 命令，因此复杂度和批量规模不变。[Bench.FunctionalTrace.ps1](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/lib/Bench.FunctionalTrace.ps1#L2652)
- 服务端匹配仍是“候选起点 × 期望窗口长度”的逐 tick 精确比较。当前窗口长度仍为 8，所以这次样本收紧不会引入可感知性能回退，只是提高了错误检出灵敏度。[NodeStateTraceService.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/NodeStateTraceService.java#L316) [NodeStateTraceService.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/NodeStateTraceService.java#L611)

## 3. 数据流或调用链
- case 在 `start_sync_latency_single_burst` 里定义 burst 序列，`collect_sync_latency_*` phase 通过 `sync_custom` 模板和 `tickCount=8` 取出该序列的 8 tick 期望窗口。[sync_samechunk_latency_1024_to_1024_banded16_single_burst.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/cases/functional/x21/sync_samechunk_latency_1024_to_1024_banded16_single_burst.json#L67) [Bench.FunctionalTrace.ps1](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/lib/Bench.FunctionalTrace.ps1#L1458)
- `Bench.FunctionalTrace.ps1` 把展开后的 `expectedPowers` 作为 `powers=` 参数传给 `redstonelink bench trace sync_latency ...`，随后统计 `matchedRatio` 和实际延迟最小/最大值。[Bench.FunctionalTrace.ps1](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/lib/Bench.FunctionalTrace.ps1#L2672) [Bench.FunctionalTrace.ps1](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/lib/Bench.FunctionalTrace.ps1#L2859)
- 服务端 `NodeStateTraceService` 在候选起点上调用 `matchesExpectedWindow(...)`，逐 tick 检查 `outputPower` 与 `active` 是否都与期望一致，任何一拍不一致都会失败。[NodeStateTraceService.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/NodeStateTraceService.java#L316) [NodeStateTraceService.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/NodeStateTraceService.java#L625)

## 4. 安全、性能、兼容性、扩展性风险分析与建议
- 安全性：本次仅调整 bench case 样本，不涉及运行时权限、存档、网络或 API 依赖，无新增安全风险。[sync_samechunk_latency_1024_to_1024_banded16_single_burst.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/cases/functional/x21/sync_samechunk_latency_1024_to_1024_banded16_single_burst.json#L1)
- 性能：样本长度仍是 8 tick，trace 批处理、命令数量和逐 tick 匹配长度都未扩大，无新增性能风险。[sync_samechunk_latency_1024_to_1024_banded16_single_burst.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/cases/functional/x21/sync_samechunk_latency_1024_to_1024_banded16_single_burst.json#L100) [NodeStateTraceService.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/NodeStateTraceService.java#L616)
- 兼容性：本次不改 `triggerSource/core` 命名、不改 `LinkNodeType` 映射、不改 trace 协议，仅让已有断言面对更严格样本；兼容风险为低。残余风险是它仍然只覆盖单个 burst 周期，若后续要证明长时间稳态固定延迟，仍建议补多周期专项。[sync_samechunk_latency_1024_to_1024_banded16_single_burst.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/cases/functional/x21/sync_samechunk_latency_1024_to_1024_banded16_single_burst.json#L117) [Bench.FunctionalTrace.ps1](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/tools/bench/lib/Bench.FunctionalTrace.ps1#L2871)

### 并发冲突审查（补充）
- 审查结论：当前提交未引入新的独立工作线程，主要仍依赖服务端主线程串行执行；并发风险重点在多入口读改写导致的后写覆盖前写，而非 JVM 级数据竞争。
- 审查范围：共享可变状态访问、批量写入原子性、事件发布链路重入风险、网络入口主线程切换边界。
- 风险等级：默认低到中；若后续把同一节点写入扩展到异步线程或跨入口并行提交，风险会升至中到高。
- 建议：新增写入口时优先补 sourceRevision/expectedRevision 乐观并发校验，并在事件分发前使用快照副本以避免可变集合外泄。
- 代码引用：沿用本审查文档第 1~4 节的代码引用。
