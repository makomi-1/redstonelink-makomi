# 提交审查：a47be73

提交时间：2026-03-31 12:22:30 +08:00  
提交主题：修复创造模式 tooltip 归属字段缺失  
提交哈希：a47be7332ec03caf6a203a3f177472b5c74397d4

## 1. 功能与语义、架构设计（关键方法和入口）
本提交采用“双路径补齐”：
1. 由各 RedstoneLink 物品在自身 tooltip 组装时补写归属字段；
2. 在创造模式容器 tooltip 构建完成后，使用客户端 mixin 作为兜底补齐。

两条路径都复用统一支持类，确保不同入口的展示语义一致。

代码引用：
- [CreativeModeInventoryScreenOriginTooltipMixin.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/client/java/com/makomi/client/mixin/CreativeModeInventoryScreenOriginTooltipMixin.java#L1)
- [CreativeTooltipOriginSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/item/CreativeTooltipOriginSupport.java#L1)

## 2. 关键数据结构和算法性能分析
实现以字段补齐与文案拼装为主，性能影响极小；统一 support 后减少多个 Item 类分散实现，维护成本下降。

代码引用：
- [LinkerItem.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/item/LinkerItem.java#L163)
- [PairableBlockItem.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/item/PairableBlockItem.java#L111)

## 3. 数据流或调用链
调用链分为两路：
1. 常规路径：物品 appendHoverText -> 统一 support 生成来源字段；
2. 兜底路径：创造模式容器 tooltip 构建完成 -> mixin 注入 -> 统一 support 补齐来源字段。

代码引用：
- [CreativeModeInventoryScreenOriginTooltipMixin.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/client/java/com/makomi/client/mixin/CreativeModeInventoryScreenOriginTooltipMixin.java#L1)
- [QuickLinkToolItem.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/item/QuickLinkToolItem.java#L68)
- [StatePanelToolItem.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/item/StatePanelToolItem.java#L59)

## 4. 安全、性能、兼容性、扩展性风险分析与建议
主要风险在 mixin 注入点兼容性，可能与其他 UI 注入模组冲突。建议保留注入最小化原则，并在冲突出现时增加条件分支保护。

代码引用：
- [redstonelink.client.mixins.json](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/resources/redstonelink.client.mixins.json#L8)
- [CreativeTooltipOriginSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/item/CreativeTooltipOriginSupport.java#L1)

### 并发冲突审查（补充）
- 审查结论：当前提交未引入新的独立工作线程，主要仍依赖服务端主线程串行执行；并发风险重点在多入口读改写导致的后写覆盖前写，而非 JVM 级数据竞争。
- 审查范围：共享可变状态访问、批量写入原子性、事件发布链路重入风险、网络入口主线程切换边界。
- 风险等级：默认低到中；若后续把同一节点写入扩展到异步线程或跨入口并行提交，风险会升至中到高。
- 建议：新增写入口时优先补 sourceRevision/expectedRevision 乐观并发校验，并在事件分发前使用快照副本以避免可变集合外泄。
- 代码引用：沿用本审查文档第 1~4 节的代码引用。
