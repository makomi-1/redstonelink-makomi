# 提交审查：3d1fd9c

提交时间：2026-04-01 12:26:48 +08:00  
提交主题：批打包内存优化 p1  
提交哈希：3d1fd9c6e61e746b399aef71c587d9171bfdd008

## 1. 功能与语义、架构设计（关键方法和入口）
该提交聚焦批调度与跨区块运行态的内存占用收敛，引入更多复用与清理策略，降低峰值阶段临时对象滞留。

代码引用：
- [CoreDispatchBatchScheduler.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CoreDispatchBatchScheduler.java#L182)
- [CrossChunkDispatchRuntimeSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CrossChunkDispatchRuntimeSupport.java#L59)

## 2. 关键数据结构和算法性能分析
主要手段为对象池复用、空闲态释放与快照 scratch 容器复用，属于典型的内存抖动优化。对吞吐影响偏正向，对实现复杂度有小幅增加。

代码引用：
- [CrossChunkDispatchService.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CrossChunkDispatchService.java#L509)
- [CrossChunkDispatchTicketSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CrossChunkDispatchTicketSupport.java#L88)
- [LinkSavedDataSerialSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/LinkSavedDataSerialSupport.java#L82)

## 3. 数据流或调用链
数据流优化点在运行时缓存生命周期：pending 快照遍历 -> ready 分组 -> 批提交 -> 空闲态回收，形成完整的“申请-使用-释放”闭环。

代码引用：
- [CrossChunkWhitelistSavedData.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CrossChunkWhitelistSavedData.java#L43)
- [LinkSavedData.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/LinkSavedData.java#L68)

## 4. 安全、性能、兼容性、扩展性风险分析与建议
风险在于复用容器的“脏状态泄漏”与释放时机错误。建议保留大样本压测回归，重点观察长时间运行后的内存曲线。

代码引用：
- [CrossChunkDispatchServiceTest.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/test/java/com/makomi/data/CrossChunkDispatchServiceTest.java#L910)
- [CoreDispatchBatchScheduler.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CoreDispatchBatchScheduler.java#L182)

### 并发冲突审查（补充）
- 审查结论：当前提交未引入新的独立工作线程，主要仍依赖服务端主线程串行执行；并发风险重点在多入口读改写导致的后写覆盖前写，而非 JVM 级数据竞争。
- 审查范围：共享可变状态访问、批量写入原子性、事件发布链路重入风险、网络入口主线程切换边界。
- 风险等级：默认低到中；若后续把同一节点写入扩展到异步线程或跨入口并行提交，风险会升至中到高。
- 建议：新增写入口时优先补 sourceRevision/expectedRevision 乐观并发校验，并在事件分发前使用快照副本以避免可变集合外泄。
- 代码引用：沿用本审查文档第 1~4 节的代码引用。
