# 提交审查：999f952

提交时间：2026-03-31 22:06:33 +08:00  
提交主题：批打包优化接入调度层 p1/2  
提交哈希：999f9527a6e08767dbe61228df4913f024e742c0

## 1. 功能与语义、架构设计（关键方法和入口）
该提交将批调度器正式接入主运行链路，建立了 direct/crosschunk/lifecycle 的统一目标级批提交入口，减少分散写入路径。

代码引用：
- [RedstoneLink.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/RedstoneLink.java#L6)
- [CoreDispatchBatchScheduler.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CoreDispatchBatchScheduler.java#L1)
- [CrossChunkDispatchRuntimeSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CrossChunkDispatchRuntimeSupport.java#L139)

## 2. 关键数据结构和算法性能分析
核心数据结构为“目标桶 + 到期 tick bucket + 同源去重条目”，通过批入队降低同 tick 重复 apply，理论上可显著减少高并发下的写扩散。

代码引用：
- [ActivatableTargetBlockEntity.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/block/entity/ActivatableTargetBlockEntity.java#L7)
- [CoreDispatchBatchScheduler.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CoreDispatchBatchScheduler.java#L1)

## 3. 数据流或调用链
调用链演化为：事件投影 -> 规则支持 -> 运行时 staging -> 统一调度 flush 到 core，链路更适合后续做窗口化与优先级控制。

代码引用：
- [InternalDispatchDeltaEvents.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/InternalDispatchDeltaEvents.java#L39)
- [InternalDispatchDeltaProjector.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/InternalDispatchDeltaProjector.java#L118)
- [InternalDispatchDeltaRuleSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/InternalDispatchDeltaRuleSupport.java#L64)

## 4. 安全、性能、兼容性、扩展性风险分析与建议
风险在于接入早期的语义一致性，特别是批处理与直发混用阶段。建议保持调度器支持集合可控并持续对齐回归矩阵。

代码引用：
- [CoreDispatchBatchScheduler.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CoreDispatchBatchScheduler.java#L1)
- [CrossChunkDispatchRuntimeSupport.java](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/src/main/java/com/makomi/data/CrossChunkDispatchRuntimeSupport.java#L139)

### 并发冲突审查（补充）
- 审查结论：当前提交未引入新的独立工作线程，主要仍依赖服务端主线程串行执行；并发风险重点在多入口读改写导致的后写覆盖前写，而非 JVM 级数据竞争。
- 审查范围：共享可变状态访问、批量写入原子性、事件发布链路重入风险、网络入口主线程切换边界。
- 风险等级：默认低到中；若后续把同一节点写入扩展到异步线程或跨入口并行提交，风险会升至中到高。
- 建议：新增写入口时优先补 sourceRevision/expectedRevision 乐观并发校验，并在事件分发前使用快照副本以避免可变集合外泄。
- 代码引用：沿用本审查文档第 1~4 节的代码引用。
