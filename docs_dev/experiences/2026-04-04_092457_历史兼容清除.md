# 历史兼容清除

## 阻塞点

- 删除跨区块持久队列的旧 `DispatchKind.ACTIVATION` 枚举后，仍有反射测试按旧常量名取值，导致全量测试在实现已正确收敛后继续失败。

## 解决方法

- 全局检索反射与字符串枚举断言，把测试改为对当前持久化枚举 `TOGGLE_EVENT/PULSE_EVENT` 建立断言，同时补一条“旧 `ACTIVATION` 持久项读档应直接丢弃”的测试，把新口径锁死。
