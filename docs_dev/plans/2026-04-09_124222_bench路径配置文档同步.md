# bench路径配置文档同步

生成时间：2026-04-09 12:42:22
文件名：2026-04-09_124222_bench路径配置文档同步.md

## 任务背景
此前已将 bench 游戏内测试的写死路径抽取到 `tools/bench/bench.path-config.json`，并完成脚本与 suite 配置复用。但中英文使用说明仍保留“脚本内置绝对路径”表述，以及若干继续显式传入旧模板路径的示例，导致文档与现状不一致，需要同步修正。

## 方案详情

### 现状分析
- 公开中文文档 `docs/使用说明.md` 的 bench 章节仍描述 `run-bench-suite.ps1` 内置 `D:\OpenProjects\RedstoneLink\mcserver` 与 `D:\OpenProjects\RedstoneLink\mcserver\rl-bench-template`。
- 公开英文文档 `docs/usage_guide_en.md` 仍保留同类硬编码默认路径说明。
- 内部中文文档 `docs_dev/使用说明.md` 的 bench 章节也未同步到新的配置化路径方案。

### 技术方案
- 在三份文档中统一引入 `tools/bench/bench.path-config.json` 作为 bench 本地路径配置来源，说明其关键字段和默认读取行为。
- 将“最短命令”示例改为依赖配置文件默认值，移除不再必要的 `-ServerRoot` / `-TemplateWorldPath` 显式参数。
- 保留可选覆盖示例，但明确这些参数现在属于显式覆盖，而不是脚本内置固定路径。
- 同步说明 suite 内 `reloadTemplateWorld` 已通过配置参数解析，不再依赖硬编码绝对路径。

### 影响范围
- `docs/使用说明.md`
- `docs/usage_guide_en.md`
- `docs_dev/使用说明.md`

## 原子步骤清单

### 步骤 1：补充路径配置说明
- **操作对象**：bench 相关使用说明章节
- **具体动作**：增加 `bench.path-config.json` 的作用、字段含义与默认读取规则说明
- **预期结果**：读者能明确 bench 路径的唯一配置入口
- **关键里程碑**：是

### 步骤 2：修正命令示例
- **操作对象**：bench suite、回归脚本、性能脚本示例
- **具体动作**：将依赖默认路径的示例调整为省略已配置参数，仅在覆盖场景保留显式参数
- **预期结果**：命令示例与当前脚本行为一致
- **关键里程碑**：是

### 步骤 3：完成文档校验与审查记录
- **操作对象**：文档差异、编码校验、审查文档
- **具体动作**：检查 BOM/乱码/差异范围，补写审查文档
- **预期结果**：文档改动可追溯且符合仓库流程要求
- **关键里程碑**：是

## 预期结果
中英文 bench 使用说明与当前脚本实现保持一致，用户可通过统一配置文件管理本地路径，并理解默认值与显式覆盖的边界。
