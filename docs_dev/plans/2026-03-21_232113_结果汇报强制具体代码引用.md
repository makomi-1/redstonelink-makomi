# 结果汇报强制具体代码引用

生成时间：2026-03-21 23:21:13
文件名：2026-03-21_232113_结果汇报强制具体代码引用.md

## 任务背景
当前仓库内协作规范要求“方案确认”和“任务汇报/结果交付”提供代码引用，但结果交付阶段仍允许仅给文件级引用、不要求具体定位。这会导致最终汇报可追溯性不足，用户仍需自行在文件内查找对应改动位置。

## 方案详情

### 现状分析
- [AGENTS.md](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/AGENTS.md) 的交流规范与文档化自动工作流仍保留“结果交付阶段仅需文件级引用，不要求行号”的要求。
- [skills/docs-dev-docflow/SKILL.md](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/skills/docs-dev-docflow/SKILL.md) 的总体原则与输出要求也同步保留了相同限制。
- 若只修改其中一个文件，另一个规则源仍可能把行为覆盖回旧口径。

### 技术方案
- 收紧 [AGENTS.md](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/AGENTS.md) 的结果汇报规则：要求“方案确认”和“任务汇报/结果交付”都给出关键改动的具体代码引用，格式继续使用 Markdown 绝对路径链接。
- 在 [AGENTS.md](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/AGENTS.md) 的报告结构段补充说明：结果汇报必须附关键改动代码引用。
- 同步收紧 [skills/docs-dev-docflow/SKILL.md](/d:/OpenProjects/RedstoneLink/rl-release-no-mixin/skills/docs-dev-docflow/SKILL.md) 中的对应条目，避免 skill 与主规则不一致。

### 影响范围
- `AGENTS.md`
- `skills/docs-dev-docflow/SKILL.md`

## 原子步骤清单

### 步骤 1：修改 AGENTS 主规则
- **操作对象**：`AGENTS.md`
- **具体动作**：更新交流规范、文档化自动工作流和报告结构，去掉“仅文件级引用”的旧口径，改为“必须带具体代码引用”
- **预期结果**：主规范明确要求每次结果汇报都附具体代码定位
- **关键里程碑**：是

### 步骤 2：同步修改 docs-dev-docflow skill
- **操作对象**：`skills/docs-dev-docflow/SKILL.md`
- **具体动作**：更新总体原则与输出要求中的引用规则，使 skill 与 AGENTS 保持一致
- **预期结果**：技能执行时不再退回“文件级即可”的旧约束
- **关键里程碑**：是

### 步骤 3：执行写后校验
- **操作对象**：本次修改文件
- **具体动作**：校验 UTF-8 无 BOM、替换字符和 diff 范围
- **预期结果**：规则文档改动范围明确且无编码污染
- **关键里程碑**：否

## 预期结果
- 以后“方案确认”和“任务汇报/结果交付”都必须提供关键改动的具体代码引用。
- 引用格式保持 Markdown 绝对路径链接不变，仅提高定位粒度。
- 主规则与 docs-dev-docflow skill 保持一致，不再出现结果汇报规则冲突。
