# 补充销毁打标回归测试

生成时间：2026-03-15 17:07:47
文件名：2026-03-15_170747_补充销毁打标回归测试.md

## 任务背景
本轮已修复“创造模式物品进岩浆未自动退役”回归，根因是 `PairableBlockItem` 的 `onDestroyed(ItemEntity)` 入口曾被误删，导致 `markDamageDiscard` 不再触发。需补充自动化测试防止同类回归。

## 方案详情

### 现状分析
- 退役判定对 `DISCARDED` 路径依赖伤害打标消费。
- 打标入口位于 `PairableBlockItem#onDestroyed(ItemEntity)`。
- 现有测试以稳定契约测试为主，适合补结构防回归测试。

### 技术方案
- 新增 `PairableBlockItemContractTest`（`stable-core`）。
- 断言 `PairableBlockItem` 声明 `onDestroyed(ItemEntity)` 方法。
- 断言该方法存在对 `LinkNodeRetireEvents.markDamageDiscard` 的符号引用（通过 `javap` 反编译输出文本校验）。

### 影响范围
- `src/test/java/com/makomi/item/PairableBlockItemContractTest.java`

## 原子步骤清单

### 步骤 1：新增契约测试类
- **操作对象**：`src/test/java/com/makomi/item/PairableBlockItemContractTest.java`
- **具体动作**：创建测试类并添加 `stable-core` 标签
- **预期结果**：测试被默认 `test` 任务发现
- **关键里程碑**：是

### 步骤 2：实现防回归断言
- **操作对象**：同上
- **具体动作**：
  1. 通过反射断言 `onDestroyed(ItemEntity)` 存在。
  2. 使用 `javap -c` 读取字节码，断言包含 `LinkNodeRetireEvents.markDamageDiscard` 调用特征。
- **预期结果**：删除入口或移除调用时测试失败
- **关键里程碑**：是

### 步骤 3：执行测试验证
- **操作对象**：Gradle
- **具体动作**：执行 `./gradlew test --tests com.makomi.item.PairableBlockItemContractTest`
- **预期结果**：新增测试通过
- **关键里程碑**：是

## 预期结果
后续如再次移除 `onDestroyed` 或移除其中 `markDamageDiscard` 调用，将在默认稳定测试中被即时拦截。
