# P2去壳清理

生成时间：2026-03-14 20:15:38
文件名：2026-03-14_201538_P2去壳清理.md

## 任务背景
当前仓库仅内部调用且未发布，P2 已完成主命名迁移。为减少冗余与告警，继续执行去壳清理：删除旧命名兼容入口。

## 方案详情

### 现状分析
- `LinkPairingScreen` 已仅作为 `TriggerSourcePairingScreen` 兼容壳。
- `PairingNetwork` 中 `openButtonPairing` 与 `OpenButtonPairingPayload` 已为兼容壳。
- 测试中仍保留对 `OpenButtonPairingPayload` 的兼容断言，导致 deprecated 告警。

### 技术方案
- 删除 `LinkPairingScreen` 文件，统一使用 `TriggerSourcePairingScreen`。
- 删除 `PairingNetwork.openButtonPairing` 兼容方法。
- 删除 `PairingNetwork.OpenButtonPairingPayload` 兼容 record。
- 调整 `PairingNetworkPayloadTest`，移除 legacy 壳测试，保留新命名契约测试。
- 协议 ID 继续保持 `open_button_pairing` 不变，避免无收益风险。

### 影响范围
- `src/client/java/com/makomi/client/screen/LinkPairingScreen.java`（删除）
- `src/main/java/com/makomi/network/PairingNetwork.java`
- `src/test/java/com/makomi/network/PairingNetworkPayloadTest.java`

## 原子步骤清单

### 步骤 1：删除 GUI 兼容壳
- **操作对象**：`LinkPairingScreen`
- **具体动作**：删除旧类文件
- **预期结果**：客户端仅保留 triggerSource 命名入口
- **关键里程碑**：是

### 步骤 2：删除网络兼容壳
- **操作对象**：`PairingNetwork`
- **具体动作**：移除旧方法与旧 payload record
- **预期结果**：网络 API 命名收口完成
- **关键里程碑**：是

### 步骤 3：同步测试
- **操作对象**：`PairingNetworkPayloadTest`
- **具体动作**：去除 legacy 壳测试并校验新命名
- **预期结果**：无 deprecated 告警来源
- **关键里程碑**：否

### 步骤 4：回归验证
- **操作对象**：编译与定向测试
- **具体动作**：执行 compile + 相关测试
- **预期结果**：行为不变且构建通过
- **关键里程碑**：否

## 预期结果
- P2 旧命名兼容壳全部清理；
- 代码命名统一为 triggerSource/core 语义；
- 编译与相关测试保持通过。
