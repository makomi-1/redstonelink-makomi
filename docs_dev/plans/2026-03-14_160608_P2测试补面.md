# P2测试补面

生成时间：2026-03-14 16:06:08
文件名：2026-03-14_160608_P2测试补面.md

## 任务背景
P2 已将 GUI 的 set_links 命令前缀从硬编码类型字符串切换为语义层统一输出（triggerSource/core）。当前需要补充自动化测试覆盖，防止后续回归。

## 方案详情

### 现状分析
- 语义 token 输出位于 LinkNodeSemantics.toCommandToken。
- GUI 侧 LinkPairingScreen/CorePairingScreen 通过该方法构建命令前缀。
- 当前缺少对 toCommandToken 的直接契约测试。

### 技术方案
- 采用最小补面方案：在稳定核心测试集中增加 toCommandToken 契约测试。
- 覆盖 BUTTON->triggersource、CORE->core、null->unknown 三个关键分支。
- 通过定向单测 + compileClientJava 做回归验证。

### 影响范围
- src/test/java/com/makomi/data/semantic/SemanticRolePolicyTest.java
- 构建验证任务（test/compileClientJava）

## 原子步骤清单

### 步骤 1：补充语义 token 契约测试
- **操作对象**：SemanticRolePolicyTest
- **具体动作**：新增 toCommandToken 测试方法，覆盖关键映射与空值兜底
- **预期结果**：P2 关键语义映射具备自动化保护
- **关键里程碑**：是

### 步骤 2：执行验证
- **操作对象**：Gradle 测试与编译任务
- **具体动作**：运行定向测试与客户端编译
- **预期结果**：测试通过，客户端侧无编译回归
- **关键里程碑**：是

## 预期结果
新增稳定、低耦合的语义 token 回归测试，确保 P2 GUI 命令语义改造在后续迭代中不被破坏。
