# 自动化测试扩展三期

生成时间：2026-03-11 20:37:44
文件名：2026-03-11_203744_自动化测试扩展三期.md

## 任务背景
在已完成基础分层与兼容回归的基础上，需要补齐“连接序号冲突”与“千级节点/链接”的性能边界验证，并先采集 1~2 轮覆盖率基线后再设置阈值。

## 方案详情

### 现状分析
- 序号冲突仅在部分分配路径覆盖，缺少放置冲突场景回归。
- slow 层尚未覆盖千级节点/链接的性能边界验证。
- 覆盖率已有报告产出，但缺少基线采集阶段的明确流程。

### 技术方案
1. 新增序号冲突回归用例，覆盖放置场景“同序列号不同坐标”与退役冲突的重分配。
2. 新增 slow 性能回归用例，构造千级节点/链接并验证保存、加载与查询耗时边界。
3. 覆盖率先保持“仅采集”，连续 1~2 轮收集报告后再设阈值。

### 影响范围
- `src/test/java/com/makomi/data/**`
- `src/test/java/com/makomi/data/**`（slow 性能用例）

## 原子步骤清单

### 步骤 1：序号冲突回归
- **操作对象**：`LinkSavedDataSerialGovernanceTest`
- **具体动作**：新增放置冲突重分配与同坐标复用测试
- **预期结果**：冲突处理可稳定回归
- **关键里程碑**：否

### 步骤 2：千级性能回归（slow）
- **操作对象**：新增 slow 性能测试类
- **具体动作**：构造千级节点/链接并验证保存/加载/查询耗时
- **预期结果**：性能边界可量化
- **关键里程碑**：否

### 步骤 3：覆盖率基线采集
- **操作对象**：流程约定
- **具体动作**：连续 1~2 轮仅收集覆盖率报告，不设阈值
- **预期结果**：获得稳定基线后再设置阈值
- **关键里程碑**：否

## 预期结果
- 连接序号冲突回归覆盖完善。
- 千级节点/链接的性能边界具备可回归数据。
- 覆盖率阈值在基线稳定后再启用。
