# 一致性确定性增强分期（P0：中继静默）

生成时间：2026-03-19 14:51:09  
文件名：2026-03-19_145109_一致性确定性增强分期_P0中继静默.md

## 任务背景
- 已确认总体分期方案：先做“中继转发不再提醒”，tick 细分后置。
- 当前实现会在跨区块接管时统一提示（含 relay 与 force-load），与新策略不一致。

## 方案详情

### 现状分析
- `TriggerSourceBlockEntity` 目前按 `hasCrossChunkHandled()` 决定是否发提示。
- `LinkedTargetDispatchService` 的提示构建会输出 force-load 和 relay 两类目标。

### 技术方案
- 只在“存在 force-load 接管”时才给玩家提示。
- relay 接管路径保持处理能力，但不再对玩家输出提示内容。
- 文档同步更新为“跨区块提示仅针对强加载接管”。

### 影响范围
- `src/main/java/com/makomi/block/entity/TriggerSourceBlockEntity.java`
- `src/main/java/com/makomi/data/LinkedTargetDispatchService.java`
- `docs_dev/使用说明.md`

## 原子步骤清单

### 步骤 1：调整提示触发条件
- **操作对象**：`TriggerSourceBlockEntity`
- **具体动作**：将提示触发条件从“任意跨区块接管”改为“仅 force-load 接管”。
- **预期结果**：relay-only 情况不再出现提示。
- **关键里程碑**：是

### 步骤 2：收敛提示内容
- **操作对象**：`LinkedTargetDispatchService`
- **具体动作**：提示消息仅保留 force-load 目标行；保留 relay 统计数据用于内部汇总。
- **预期结果**：玩家侧不再看到 relay 目标列表。
- **关键里程碑**：是

### 步骤 3：文档同步
- **操作对象**：`docs_dev/使用说明.md`
- **具体动作**：更新“跨区块接管提示”章节，说明 relay 不提示。
- **预期结果**：文档与行为一致。
- **关键里程碑**：否

### 步骤 4：回归验证
- **操作对象**：Gradle 测试任务
- **具体动作**：执行全标签自动化测试。
- **预期结果**：构建与测试通过；若性能测试偶发波动，单测复核记录原因。
- **关键里程碑**：是

## 预期结果
- relay 转发保持可用但不打扰玩家提示。
- force-load 接管提示保留，运维可见性不丢失。
- 不改变 `triggerSource/core` 命名与方向语义，不引入 `com.makomi.api.v1.*`。
