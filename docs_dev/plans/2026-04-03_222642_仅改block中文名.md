# 仅改 block 中文名

生成时间：2026-04-03 22:26:42
文件名：2026-04-03_222642_仅改block中文名.md

## 任务背景
用户要求在现有中文命名调整基础上，仅继续修改 block 中文名；item 与 screen 不再改动。

## 方案详情

### 现状分析
当前 `src/main/resources/assets/redstonelink/lang/zh_cn.json` 中 block 条目仍保留旧命名（如“连接红石核心”），与 item 新命名风格不一致。

### 技术方案
仅在 `zh_cn.json` 修改 10 个 `block.redstonelink.*` 条目 value：
1. 前 4 项改为“连接核心块/连接核心片”及透明变体。
2. 其余 6 项去掉“连接”前缀，保持与 item 同风格。
3. 不改任何 key，不改 item 文案，不改 screen 文案。

### 影响范围
- 仅影响：`src/main/resources/assets/redstonelink/lang/zh_cn.json`
- 影响场景：方块名称显示（物品栏方块态、提示文本等）。

## 原子步骤清单

### 步骤 1：修改 block 核心命名
- **操作对象**：`zh_cn.json`
- **具体动作**：将 core 与 dust core 的 4 个 block 名称改为“连接核心块/连接核心片”。
- **预期结果**：核心方块命名与最新术语一致。
- **关键里程碑**：是

### 步骤 2：修改其余 block 名称
- **操作对象**：`zh_cn.json`
- **具体动作**：将按钮、拉杆、发射器等 6 个 block 名称去掉“连接”前缀。
- **预期结果**：命名风格与 item 保持一致。
- **关键里程碑**：是

### 步骤 3：执行基础校验
- **操作对象**：`zh_cn.json`
- **具体动作**：校验 JSON 语法与目标键命中结果。
- **预期结果**：无语法错误，改动范围准确。
- **关键里程碑**：否

## 预期结果
在不影响 key 与逻辑的前提下，block 中文名与 item 新命名保持一致，避免前后文案风格割裂。
