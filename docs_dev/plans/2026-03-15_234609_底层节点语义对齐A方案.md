# 底层节点语义对齐（A 直接对齐）

生成时间：2026-03-15 23:46:09
文件名：2026-03-15_234609_底层节点语义对齐A方案.md

## 任务背景
用户确认执行“选项 A”：将底层节点语义从 `BUTTON/CORE` 对齐为 `TRIGGER_SOURCE/CORE`，并明确当前阶段无外部兼容风险、无存档兼容风险。

## 方案详情

### 现状分析
- `LinkNodeType.BUTTON` 在 `main/client/test` 广泛使用，属于高影响重构。
- 语义规则与别名解析依赖 `BUTTON` 常量判断，需要同步替换。
- 存档与命令标准词表基于 `triggerSource/core`，不依赖 `BUTTON` 字面值。

### 技术方案
- 将 `LinkNodeType` 枚举常量 `BUTTON` 重命名为 `TRIGGER_SOURCE`。
- 全仓替换 `LinkNodeType.BUTTON` 为 `LinkNodeType.TRIGGER_SOURCE`。
- 同步修正 `switch/case`、语义策略、映射器与相关注释。
- 不引入重兼容层；仅保留现有对 `triggerSource` 文本语义的解析能力。

### 影响范围
- 数据语义层：`com.makomi.data.*` 与 `com.makomi.data.semantic.*`
- 命令与网络层：`com.makomi.command.*`、`com.makomi.network.*`
- 方块与物品：`com.makomi.block.*`、`com.makomi.item.*`
- API 映射层：`com.makomi.api.v1.model.*`、`com.makomi.api.v1.internal.*`
- 客户端与测试代码：`src/client/java`、`src/test/java`

## 原子步骤清单

### 步骤 1：调整枚举定义
- **操作对象**：`LinkNodeType`
- **具体动作**：将 `BUTTON` 重命名为 `TRIGGER_SOURCE`，并更新注释语义说明。
- **预期结果**：底层类型定义与新语义一致。
- **关键里程碑**：是

### 步骤 2：全仓引用替换
- **操作对象**：`main/client/test` Java 文件
- **具体动作**：批量替换 `LinkNodeType.BUTTON` 引用，并修复 `switch` 分支与映射逻辑。
- **预期结果**：代码可编译，类型引用统一。
- **关键里程碑**：是

### 步骤 3：语义映射校正
- **操作对象**：语义策略、别名解析、命令令牌映射
- **具体动作**：将角色规则与语义名输出改为基于 `TRIGGER_SOURCE`。
- **预期结果**：`triggerSource/core` 解析与校验行为保持正确。
- **关键里程碑**：是

### 步骤 4：回归验证
- **操作对象**：Gradle 测试任务
- **具体动作**：执行全标签自动化测试 `test testIntegration testClient testSlow testApiLegacy`。
- **预期结果**：回归通过或输出明确失败点。
- **关键里程碑**：是

## 预期结果
- 底层节点常量完成语义对齐：`TRIGGER_SOURCE/CORE`。
- 现有 `triggerSource/core` 对外语义保持一致。
- 全标签自动化测试通过，具备可回归证据。
