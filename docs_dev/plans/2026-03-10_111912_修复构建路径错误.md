# 修复构建路径错误

生成时间：2026-03-10 11:19:20
文件名：2026-03-10_111912_修复构建路径错误.md

## 任务背景
Java 项目因缺失 `src/main/generated` 源目录导致构建路径错误，影响本地构建与 IDE 识别。

## 方案详情

### 现状分析
- Java 构建路径报错提示缺失 `src/main/generated`。
- Fabric Loom 数据生成配置使 IDE/Gradle 期望该目录存在。

### 技术方案
- 创建 `src/main/generated` 目录并放置 `.gitkeep` 占位文件，确保目录被版本控制。
- 不改动构建逻辑，保持改动最小化。

### 影响范围
- 新增 `src/main/generated/.gitkeep`。
- 更新 `agents/plans/05_后续迭代与测试路线.md` 的更新记录。

## 原子步骤清单

### 步骤 1：方案文档化
- **操作对象**：`/plan/2026-03-10_111912_修复构建路径错误.md`
- **具体动作**：记录背景、方案与步骤
- **预期结果**：方案可追溯
- **关键里程碑**：是

### 步骤 2：创建生成目录
- **操作对象**：`src/main/generated/.gitkeep`
- **具体动作**：创建目录并新增占位文件
- **预期结果**：构建路径错误消失
- **关键里程碑**：是

### 步骤 3：更新 05 路线记录
- **操作对象**：`agents/plans/05_后续迭代与测试路线.md`
- **具体动作**：新增更新条目
- **预期结果**：路线记录同步
- **关键里程碑**：否

### 步骤 4：版本号审查
- **操作对象**：`docs/experience/版本号设计参考.md`、`gradle.properties`
- **具体动作**：判定是否升版并形成结论
- **预期结果**：版本治理合规
- **关键里程碑**：否

## 预期结果
IDE 构建路径错误消除，工程可正常构建。