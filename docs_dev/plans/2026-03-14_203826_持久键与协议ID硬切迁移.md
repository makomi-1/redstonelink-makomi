# 持久键与协议ID硬切迁移

生成时间：2026-03-14 20:38:26
文件名：2026-03-14_203826_持久键与协议ID硬切迁移.md

## 任务背景
项目当前仅内部调用，客户端与服务端无外部资产约束。用户确认采用硬切策略：直接迁移持久化键与网络协议 ID，不保留兼容层。

## 方案详情

### 现状分析
- `LinkSavedData` 链接条目仍使用历史键名 `buttonSerial/coreSerials`。
- 触发源配对网络协议 ID 仍使用历史命名 `open_button_pairing`。
- 现有测试 fixture 与兼容测试样本包含上述历史键名。

### 技术方案
- 持久化键硬切为：`sourceSerial/targetSerials`。
- 触发源协议 ID 硬切为：`open_triggersource_pairing`。
- 不保留旧键/旧协议双读写与双注册。
- 同步更新受影响 fixture 与测试断言，确保当前版本闭环通过。

### 影响范围
- `src/main/java/com/makomi/data/LinkSavedData.java`
- `src/main/java/com/makomi/network/PairingNetwork.java`
- `src/test/resources/fixtures/nbt/link_saved_data_invalid_dimension.snbt`
- `src/test/resources/fixtures/nbt/link_saved_data_missing_fields.snbt`
- `src/test/java/com/makomi/data/LinkSavedDataLoadCompatibilityTest.java`

## 原子步骤清单

### 步骤 1：修改持久化键
- **操作对象**：`LinkSavedData`
- **具体动作**：将 links 条目键名改为 `sourceSerial/targetSerials`
- **预期结果**：存档键名对齐新语义
- **关键里程碑**：是

### 步骤 2：修改触发源协议 ID
- **操作对象**：`PairingNetwork`
- **具体动作**：`OpenTriggerSourcePairingPayload.TYPE` 改为 `open_triggersource_pairing`
- **预期结果**：网络协议命名对齐 triggerSource
- **关键里程碑**：是

### 步骤 3：更新测试样本
- **操作对象**：fixture 与 load 测试
- **具体动作**：替换旧键名样本与断言
- **预期结果**：测试数据与新契约一致
- **关键里程碑**：否

### 步骤 4：定向回归
- **操作对象**：编译与核心测试
- **具体动作**：执行 `compileJava` 与 `LinkSavedData/PairingNetwork` 相关测试
- **预期结果**：迁移后行为稳定
- **关键里程碑**：否

## 预期结果
- 持久化键与网络协议 ID 完成硬切；
- 当前版本不再兼容旧键/旧协议；
- 构建与相关测试通过。
