# 核心模块未使用成员清理

生成时间：2026-04-27 08:52:31
文件名：2026-04-27_085231_核心模块未使用成员清理.md

## 任务背景
用户要求继续清理 IDE 在核心实现文件中提示的未使用导入、方法或值。范围从上一轮的 quick-link / pairing 相关文件，进一步扩大到 QTL outline render、配对 screen、网络、图写、状态面板以及相关核心实现。目标是只处理确认可删的未使用私有成员，不顺带调整业务逻辑。

## 方案详情

### 现状分析
- 已对以下范围做主实现文件扫描：
  - QTL outline render
  - 配对 screen / quick-link screen
  - 网络层主实现
  - 图写主实现
  - 状态面板主实现
- 当前确认可稳定删除的成员共 6 个：
  1. `QuickLinkOutlineRenderer.addExposedFaceCell(...)`
  2. `QuickLinkOutlineRenderer.toggleEdge(...)`
  3. `QuickLinkOutlineRenderer.toLineSegmentKey(...)`
  4. `QuickLinkToolScreen.MODE_LINE_MARGIN`
  5. `GraphWriteService.totalLinkCommandCost(...)`
  6. `StatePanelToolScreen.centeredTextBounds(...)`
- 其余本轮扫描到的网络、图写、状态面板主实现文件里，没有再发现可信的未使用导入、方法或值，不建议为清告警硬删成员。

### 技术方案
- 仅删除“私有且本文件内无引用”的方法或常量。
- 不修改任何业务判断、协议字段、屏幕布局语义、图写流程或状态面板数据流。
- 不把测试源集纳入本轮清理，避免将测试替身、夹具或断言辅助误判为未使用。

### 影响范围
- `src/client/java/com/makomi/client/render/QuickLinkOutlineRenderer.java`
- `src/client/java/com/makomi/client/screen/QuickLinkToolScreen.java`
- `src/main/java/com/makomi/data/GraphWriteService.java`
- `src/client/java/com/makomi/client/screen/StatePanelToolScreen.java`

## 原子步骤清单

### 步骤 1：清理 QTL outline render 死私有方法
- **操作对象**：`QuickLinkOutlineRenderer`
- **具体动作**：删除 3 个未使用的私有辅助方法
- **预期结果**：outline render 不再保留旧实现残片
- **关键里程碑**：是

### 步骤 2：清理 quick-link screen 死常量
- **操作对象**：`QuickLinkToolScreen`
- **具体动作**：删除未参与布局计算的 `MODE_LINE_MARGIN`
- **预期结果**：screen 常量与实际使用保持一致
- **关键里程碑**：否

### 步骤 3：清理 graph write 死私有方法
- **操作对象**：`GraphWriteService`
- **具体动作**：删除未被调用的成本辅助方法 `totalLinkCommandCost(...)`
- **预期结果**：图写服务中不再保留失效私有辅助实现
- **关键里程碑**：否

### 步骤 4：清理状态面板 screen 死私有方法
- **操作对象**：`StatePanelToolScreen`
- **具体动作**：删除未被调用的 `centeredTextBounds(...)`
- **预期结果**：状态面板 screen 去掉死辅助方法
- **关键里程碑**：否

### 步骤 5：回归验证
- **操作对象**：当前工作区
- **具体动作**：执行 `.\gradlew.bat test testIntegration testClient testSlow --no-daemon`
- **预期结果**：确认 unused 清理未引入编译或行为回归
- **关键里程碑**：是

## 预期结果
本轮只删除核心实现文件里已确认的 6 个未使用成员，不改变业务行为；IDE 在 QTL outline render、quick-link screen、graph write、状态面板相关核心文件中的明显 unused 告警将减少，并通过全标签自动化测试回归验证。
