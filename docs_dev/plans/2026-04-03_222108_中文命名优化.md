# 中文命名优化

生成时间：2026-04-03 22:21:08
文件名：2026-04-03_222108_中文命名优化.md

## 任务背景
用户要求先前预览方案落盘：将中文名“红石连接”调整为“红石远联”，并按指定规则调整部分物品中文名；其余物品名按已确认预览执行。

## 方案详情

### 现状分析
当前中文文案集中在 `src/main/resources/assets/redstonelink/lang/zh_cn.json`，本次仅改语言文案，不改 key、不改逻辑、不改资源路径。

### 技术方案
在单文件内做最小粒度替换：
1. 定向替换 3 处“红石连接”相关文案。
2. 定向调整 13 个 `item.*` 名称，其中前 4 项按用户新命名（核心块/核心片），其余 9 项按先前预览移除“连接”前缀。
3. 保持 `block.*` 文案不变，避免超出本次确认范围。

### 影响范围
- 仅影响：`src/main/resources/assets/redstonelink/lang/zh_cn.json`
- 可能影响：创造栏、物品 tooltip/名称显示、配对界面标题显示。

## 原子步骤清单

### 步骤 1：替换组名与界面标题
- **操作对象**：`zh_cn.json`
- **具体动作**：替换 `itemGroup.redstonelink` 与 `screen.redstonelink.pairing.title` 文案。
- **预期结果**：显示名改为“红石远联”“连接配对”。
- **关键里程碑**：是

### 步骤 2：替换原件名称
- **操作对象**：`zh_cn.json`
- **具体动作**：将 `item.redstonelink.redstone_link_component` 文案改为“连接原件”。
- **预期结果**：原件名称按用户指定显示。
- **关键里程碑**：否

### 步骤 3：替换 13 个 item 名称
- **操作对象**：`zh_cn.json`
- **具体动作**：前 4 项改为“连接核心块/连接核心片”及透明变体；其余 9 项按预览去掉“连接”前缀。
- **预期结果**：物品命名全部符合本轮确认。
- **关键里程碑**：是

### 步骤 4：核对 JSON 有效性
- **操作对象**：`zh_cn.json`
- **具体动作**：检查语法与关键项命中，确认仅文案改动。
- **预期结果**：无语法错误、改动范围可控。
- **关键里程碑**：否

## 预期结果
中文 UI/物品名按用户确认方案更新，不引入代码语义变化与兼容风险。
