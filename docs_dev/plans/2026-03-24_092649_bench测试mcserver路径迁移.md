# bench 测试 mcserver 路径迁移

生成时间：2026-03-24 09:26:49
文件名：2026-03-24_092649_bench测试mcserver路径迁移.md

## 任务背景
外部 dedicated server 已从旧目录迁移到 `D:\OpenProjects\RedstoneLink\mcserver`。
当前 bench/功能测试脚本本身没有硬编码旧路径，但使用说明中的命令示例仍然停留在旧目录，suite 脚本也缺少与当前工作站环境对齐的默认值，导致每次执行都需要手工改路径。

## 方案详情

### 现状分析
- `tools/bench/run-bench-suite.ps1` 的 `ServerRoot` 与 `TemplateWorldPath` 目前完全依赖外部传参。
- `tools/bench/run-bench.ps1` 不直接依赖 dedicated server 根目录，不需要改动。
- `docs_dev/使用说明.md` 中 bench suite 相关示例仍使用旧的 `C:\Users\15166\Desktop\mcserver` 路径。

### 技术方案
- 为 `run-bench-suite.ps1` 增加当前本机环境可直接使用的默认 `ServerRoot` 与 `TemplateWorldPath`。
- 保留显式传参优先，避免破坏脚本通用性。
- 同步更新 `docs_dev/使用说明.md` 中所有 bench suite 示例路径，统一改为 `D:\OpenProjects\RedstoneLink\mcserver`。

### 影响范围
- `tools/bench/run-bench-suite.ps1`
- `docs_dev/使用说明.md`

## 原子步骤清单

### 步骤 1：更新 suite 默认路径
- **操作对象**：`tools/bench/run-bench-suite.ps1`
- **具体动作**：为 `ServerRoot`、`TemplateWorldPath` 设置新的 dedicated server 默认目录
- **预期结果**：本机执行 suite 时可少传路径参数
- **关键里程碑**：是

### 步骤 2：更新使用说明路径示例
- **操作对象**：`docs_dev/使用说明.md`
- **具体动作**：将 bench suite 相关示例中的旧 `mcserver` 路径替换为新目录
- **预期结果**：文档与当前实际环境一致
- **关键里程碑**：是

### 步骤 3：校验改动范围
- **操作对象**：上述改动文件
- **具体动作**：检查 diff，确认仅涉及 bench 路径入口与说明
- **预期结果**：无业务逻辑变更
- **关键里程碑**：否

## 预期结果
bench suite 的本机默认入口与新的 `mcserver` 目录保持一致；使用说明中的复制即用命令不再指向过期路径；不影响现有 `triggerSource/core` 语义、方向和测试逻辑。
