# 规则
- 该文档自动更新

# 涉及内容
- 每次版本号更新

# 格式说明：
例如：
说明：以下示例仅用于格式参考，不计入 `# 正文` 记录。
规则：`# 正文` 下仅存放同结构的独立记录，且每条记录使用一个 `## 时间` 标题，不与格式说明混写。
## 2026.3.10
### 序号（同一天内） 具体版本号
- 更新要点：
1. 
2. 
3. 

# 正文
## 2026.3.11
### 1 版本更新
- 0.5.0-alpha -> 0.5.1-alpha
- 更新要点：
1. 物品栏“当前连接”提示按字符数截断并显示 `…(+N)`。
2. 新增 `TooltipTextTruncateUtil` 统一截断逻辑。
- 依据：
1. 展示层优化，兼容性不变，符合 PATCH 升版。
