# 05 后续迭代与测试路线（基于 redstonelink_plan.txt）

## 目标
将当前零散待办收敛为可执行迭代计划，确保每一项都有明确实现边界、验证方式和交付顺序。

## 状态总览

### 已完成（与当前代码一致）
1. 红石连接原件已实现并可用于合成改造对象。
2. 三套基础配方已落地（连接原件/核心/按钮）。
3. 核心与按钮在创造页与搜索中可见。
4. 多对多配对、序号台账、退役/审计命令已落地。
5. 流体冲刷移除不再直接退役：核心/按钮 `onRemove` 改为统一 `unregisterNode`，退役仅留给可判定销毁路径。
6. 交互约束细化已完成：已放置核心/按钮需“潜行 + 双手空手（主手空 + 副手空）”打开配对 UI。
7. 新增“连接核心-红石粉”阶段A骨架：方块/物品/方块实体注册、序号配对接入、基础资源与命名已完成；基础渲染与物品图标已对齐原版红石粉。

### 待实现（来自待办清单）
1. 配方自动化：为同类核心/按钮变体建立可批量生成流程（数据生成器或脚本）。
2. [x] 许可证文件补充（README / LICENSE 一致性检查，已切换 GPL-3.0 并补充风险声明 README）。
3. 连接核心-红石粉阶段C：扩展核心红石粉可附着在六面：
    1. 现在已经能附着在正上面，需要扩展到侧面和底面
    2. 扩展的附着的方块为激活对象
    3. 模型贴图复用已有正上方的
    4. [x] 侧面和底面附着不参与与其它红石粉连接交互
    5. [x] （已放弃）侧面和底面附着参与与其它红石粉连接交互（原因：原版红石连接机制限制）
    6. （已跳过）顶格不参与形态演算问题看，原版neighborChanged()强耦合
    
5. [x] 添加按钮类：拉杆 切换 / 木按钮 脉冲，设计逻辑跟已有的按钮实现一样（已实现 `link_toggle_lever` / `link_push_button`；其中脉冲定义为“先激活后熄灭”）
6. [x] 核心类添加纯透明贴图变种（已完成红石块核心与红石粉核心透明子类，并补齐对应原物品 1:1 直接合成配方）。
7. Mod所有物品的贴图模型颜色改为{红->蓝，木头、石头材质添加一层50%透明度的蓝色覆盖}：
    0. 改造的层级为贴图级，不是渲染逻辑
    1. 实现核心红石块的改变
    2. 实现核心红石粉的改变
8. 贴图资源本地化，提高稳定性
------发布1.0apha版本
9. 堆叠问题专项治理与回归测试。
10. 自动清理策略完善 + 台账退役回归测试。
11. 配置文件支持（config）与可调参数接入。
12. 服务端专项审查与测试。
13. 完整回归审查与交付验收。
------发布1.0beta版本

------发布1.0版本


- [x] 阶段B实施：红石粉核心先切回“完全继承原版红石粉状态机”（`RedStoneWireBlock`），核心激活状态转入方块实体管理，后续在此基线上增量重写。
- [x] 阶段B实施：核心激活改为直接驱动原版红石粉 POWER（15/0），并在激活时拦截邻居重算防止 POWER 被原版逻辑回落。
- [x] 阶段B修正：核心红石粉改为“仅输出信号不接收输入”，邻居变化与放置时统一按核心激活态强制同步 POWER（15/0）。
- [x] 阶段C-1实施：核心红石粉支持六面附着，侧面与底面附着时连接态固定为 `none`，仅保留核心激活输出并复用现有贴图。
- [x] 阶段C-1修正：修复 blockstates 条件结构导致的紫黑材质问题；非顶面附着时仅向 `support_face` 相反方向输出信号，激活对象对齐附着方向。
- [x] 阶段C-1修正：侧面与底面附着时覆盖 `updateShape/isSignalSource`，彻底退出与普通/核心红石粉的连接判定与形状联动。
- [x] 阶段D-1实施：新增连接拉杆（切换）与连接木按钮（脉冲）两类按钮源，复用原有配对与触发链路并补齐注册、资源与配方。
- [x] 阶段D-1修正：补齐触发模式链路，木按钮触发按 `PULSE` 生效（先激活后熄灭），拉杆/石按钮保持 `TOGGLE` 语义。
- [x] 阶段D-1规范化：石按钮改为与木按钮一致的“专用子类+专用方块实体”写法，`LinkButtonBlock/LinkButtonBlockEntity` 仅保留公共基类职责。
